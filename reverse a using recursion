#include <stdio.h>
#include <string.h>
 
void revstr(char*, int, int);
 
int main()
{
    char str[100];

 printf("Enter The String: "); 
    gets(str);
 
    revstr(str, 0, strlen(str)-1);
 
    printf("%s\n", str);
 
    return 0;
    }
